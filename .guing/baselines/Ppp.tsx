// @guing:generated-baseline hash:81f0c5856586
// Do not edit this file — it is used by guing for 3-way merge on re-sync
import React from 'react';
import { MuiBox, MuiTypography } from '../adapters/mui/internal';

export interface PppProps {
  label?: string;
  icon?: React.ReactNode;
  iconPosition?: 'left' | 'right';
  disabled?: boolean;
  state?: 'default' | 'pressed' | 'disabled';
  variant?: 'default' | 'pressed' | 'disabled';
  onClick?: () => void;
  onPress?: () => void;
  onRelease?: () => void;
  onHover?: () => void;
  isDisabled?: boolean;
  isLoading?: boolean;
  children?: React.ReactNode;
  sx?: Record<string, unknown>;
  className?: string;
}

const getBackgroundForVariant = (variant: 'default' | 'pressed' | 'disabled'): string => {
  switch (variant) {
    case 'pressed':
      return 'var(--color-grey-page, #051321)';
    case 'disabled':
      return 'var(--color-neutral-300, #D1D5DB)';
    case 'default':
    default:
      return 'linear-gradient(97deg, rgba(58,62,123,1) 0%, rgba(97,74,176,1) 56.5%, rgba(155,93,255,1) 100%)';
  }
};

const getTextColorForVariant = (variant: 'default' | 'pressed' | 'disabled'): string => {
  switch (variant) {
    case 'disabled':
      return 'var(--color-grey-secondary-body-text, #90A3B1)';
    case 'pressed':
    case 'default':
    default:
      return 'var(--color-white-white, #FFFFFF)';
  }
};

const ppp = React.forwardRef<HTMLDivElement, PppProps>((
  {
    label = 'Confirm',
    icon,
    iconPosition = 'left',
    disabled = false,
    state = 'default',
    variant,
    onClick,
    onPress,
    onRelease,
    onHover,
    isDisabled = false,
    isLoading = false,
    children,
    sx,
    className,
  },
  ref
) => {
  const resolvedVariant: 'default' | 'pressed' | 'disabled' =
    variant ??
    (disabled || isDisabled ? 'disabled' : state === 'pressed' ? 'pressed' : 'default');

  const isInteractionDisabled = resolvedVariant === 'disabled' || disabled || isDisabled;

  const background = getBackgroundForVariant(resolvedVariant);
  const textColor = getTextColorForVariant(resolvedVariant);

  const handleClick = () => {
    if (isInteractionDisabled) return;
    onClick?.();
  };

  const handleMouseDown = () => {
    if (isInteractionDisabled) return;
    onPress?.();
  };

  const handleMouseUp = () => {
    if (isInteractionDisabled) return;
    onRelease?.();
  };

  const handleMouseEnter = () => {
    if (isInteractionDisabled) return;
    onHover?.();
  };

  return (
    <MuiBox
      ref={ref}
      data-figma-component="Ppp"
      className={className}
      onClick={handleClick}
      onMouseDown={handleMouseDown}
      onMouseUp={handleMouseUp}
      onMouseEnter={handleMouseEnter}
      role="button"
      tabIndex={isInteractionDisabled ? -1 : 0}
      aria-disabled={isInteractionDisabled}
      sx={{
        width: '108px',
        height: '44px',
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'center',
        padding: 'var(--gap-space-xs, 8px) var(--gap-space-xl, 24px)',
        borderRadius: '12px',
        background,
        boxShadow: resolvedVariant === 'default' ? '0px 1.5px 3px 0px rgba(22,5,50,0.2)' : 'none',
        cursor: isInteractionDisabled ? 'not-allowed' : 'pointer',
        pointerEvents: isInteractionDisabled ? 'none' : 'auto',
        userSelect: 'none',
        boxSizing: 'border-box',
        transition: 'background 0.15s ease, box-shadow 0.15s ease',
        '&:hover': {
          opacity: isInteractionDisabled ? 1 : 0.92,
        },
        ...sx,
      }}
    >
      <MuiBox
        sx={{
          width: 'fit-content',
          height: 'fit-content',
          minHeight: 'fit-content',
          display: 'flex',
          flexDirection: 'row',
          alignItems: 'center',
          justifyContent: 'center',
          gap: 'var(--gap-space-xxs, 4px)',
        }}
      >
        {icon && iconPosition === 'left' && (
          <MuiBox
            sx={{
              display: 'flex',
              alignItems: 'center',
              color: textColor,
              opacity: isInteractionDisabled ? 0.5 : 1,
            }}
          >
            {icon}
          </MuiBox>
        )}
        <MuiTypography
          sx={{
            fontSize: 'var(--font-headline-med-headline-reg-label-large-14-med-size, 14px)',
            fontWeight: 'var(--font-headline-med-headline-reg-label-large-14-med-weight, 500)',
            fontFamily: 'var(--font-headline-med-headline-reg-label-large-14-med-family, Inter)',
            lineHeight: 'normal',
            color: textColor,
            textAlign: 'center',
          }}
        >
          {label}
        </MuiTypography>
        {icon && iconPosition === 'right' && (
          <MuiBox
            sx={{
              display: 'flex',
              alignItems: 'center',
              color: textColor,
              opacity: isInteractionDisabled ? 0.5 : 1,
            }}
          >
            {icon}
          </MuiBox>
        )}
        {children}
      </MuiBox>
    </MuiBox>
  );
});

ppp.displayName = 'Ppp';

export { ppp };
