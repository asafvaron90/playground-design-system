# test-client-design-system — AI Coding Instructions
# Generated by Guing v0.0.4 on 2026-03-14T13:36:31.966Z

## Package
Always import from `test-client-design-system`:
```tsx
import { Button, Text } from 'test-client-design-system';
import 'test-client-design-system/src/design-tokens/tokens.css';
```

## Design Token Rules
- NEVER hardcode hex colors — use CSS variables: `var(--color-primary)`
- NEVER hardcode font sizes — use CSS variables: `var(--font-heading-1-size)`
- For TypeScript access: `import { designTokens } from 'test-client-design-system'`

## Available Components (4 total)

### Button
  Button component
  Import: `import { Button } from 'test-client-design-system'`
  Props:
    (no props)

### IconText
  IconText component
  Import: `import { IconText } from 'test-client-design-system'`
  Props:
    - icon: ReactNode (required) — The icon to display, typically an SVG or an icon component
    - text: string (required) — The text to display next to the icon
    - size: 'small' | 'medium' | 'large' (optional) — The size of the component
- 'small': 12px font size
- 'medium': 14px font size
- 'large': 16px font size
    - variant: 'default' | 'primary' (optional) — The variant of the component
- 'default': standard style
- 'primary': primary style
    - disabled: boolean (optional) — Whether the component is disabled
    - className: string (optional) — Additional class name for custom styling

### InputsControls
  InputsControls component
  Import: `import { InputsControls } from 'test-client-design-system'`
  Props:
    - children: ReactNode (required) — The content of the InputsControls component.
    - size: 'small' | 'medium' | 'large' (optional) — The size of the component.
'small' | 'medium' | 'large'
    - variant: 'default' | 'outlined' (optional) — The variant of the component.
'default' | 'outlined'
    - disabled: boolean (optional) — If true, the component is disabled.
    - className: string (optional) — Additional class name for styling overrides.

### MenuActionItemSingle
  MenuActionItemSingle component
  Import: `import { MenuActionItemSingle } from 'test-client-design-system'`
  Props:
    - size: 'small' | 'medium' | 'large' (optional) — The size of the menu item
    - variant: 'default' | 'primary' | 'secondary' (optional) — The variant of the menu item
    - disabled: boolean (optional) — Whether the menu item is disabled
    - className: string (optional) — Additional class name for custom styling

## Token Summary
- Colors: 33 tokens
- Typography: 20 tokens
- Radius: 18 tokens
- Shadows: 2 tokens
- Elevations: 1 tokens
- Borders: 29 tokens
- Sizes: 13 tokens
- Opacity: 12 tokens
- Gradients: 8 tokens
- Blurs: 2 tokens

## Component Conventions
- All components accept a `className` prop for overrides
- Use `forwardRef` for DOM element access
- Include JSDoc for all public props
