# test-client-design-system — AI Coding Instructions
# Generated by Guing v0.0.4 on 2026-03-14T13:55:33.335Z

## Package
Always import from `test-client-design-system`:
```tsx
import { Button, Text } from 'test-client-design-system';
import 'test-client-design-system/src/design-tokens/tokens.css';
```

## Design Token Rules
- NEVER hardcode hex colors — use CSS variables: `var(--color-primary)`
- NEVER hardcode font sizes — use CSS variables: `var(--font-heading-1-size)`
- For TypeScript access: `import { designTokens } from 'test-client-design-system'`

## Available Components (4 total)

### Button
  Button component
  Import: `import { Button } from 'test-client-design-system'`
  Props:
    (no props)

### IconTextButton
  IconTextButton component
  Import: `import { IconTextButton } from 'test-client-design-system'`
  Props:
    - size: 'small' | 'medium' | 'large' (optional) — The size of the button
    - variant: 'primary' | 'secondary' | 'tertiary' (optional) — The variant of the button
    - disabled: boolean (optional) — If true, the button will be disabled
    - text: string (required) — The text to display inside the button
    - icon: React.ReactNode (required) — The icon to display inside the button
    - className: string (optional) — Additional class names to apply to the button

### InputsControls
  InputsControls component
  Import: `import { InputsControls } from 'test-client-design-system'`
  Props:
    - size: 'small' | 'medium' | 'large' (optional) — The size of the input control
    - variant: 'default' | 'outlined' (optional) — The variant of the input control
    - state: 'normal' | 'disabled' | 'error' (optional) — The state of the input control
    - className: string (optional) — Additional class name for styling overrides

### MenuActionItem
  MenuActionItem component
  Import: `import { MenuActionItem } from 'test-client-design-system'`
  Props:
    - size: 'small' | 'medium' | 'large' (optional) — The size of the menu action item
    - variant: 'default' | 'primary' | 'secondary' (optional) — The variant of the menu action item
    - state: 'normal' | 'disabled' | 'active' (optional) — The state of the menu action item
    - className: string (optional) — Additional class name for custom styling

## Token Summary
- Colors: 33 tokens
- Typography: 20 tokens
- Radius: 18 tokens
- Shadows: 2 tokens
- Elevations: 1 tokens
- Borders: 29 tokens
- Sizes: 13 tokens
- Opacity: 12 tokens
- Gradients: 8 tokens
- Blurs: 2 tokens

## Component Conventions
- All components accept a `className` prop for overrides
- Use `forwardRef` for DOM element access
- Include JSDoc for all public props
