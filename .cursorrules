# test-client-design-system — AI Coding Instructions
# Generated by Guing v0.0.2 on 2026-03-04T23:26:38.990Z

## Package
Always import from `test-client-design-system`:
```tsx
import { Button, Text } from 'test-client-design-system';
import 'test-client-design-system/src/design-tokens/tokens.css';
```

## Design Token Rules
- NEVER hardcode hex colors — use CSS variables: `var(--color-primary)`
- NEVER hardcode font sizes — use CSS variables: `var(--font-heading-1-size)`
- For TypeScript access: `import { designTokens } from 'test-client-design-system'`

## Available Components (8 total)

### Button
  Button component
  Import: `import { Button } from 'test-client-design-system'`
  Props:
    - variant: 'primary' | 'secondary' | 'ghost' (optional) — The variant of the button.
    - size: 'sm' | 'md' | 'lg' (optional) — The size of the button.
    - loading: boolean (optional) — If true, the button will show a loading spinner.
    - disabled: boolean (optional) — If true, the button will be disabled.
    - className: string (optional) — Additional className for custom styling.

### Text
  Text component for displaying typographic elements.
  Import: `import { Text } from 'test-client-design-system'`
  Props:
    - variant: keyof typeof typographyVariants (optional) — The variant of the text.
    - className: string (optional) — Additional className for custom styling.

### ColorSwatch
  ColorSwatch component
  Import: `import { ColorSwatch } from 'test-client-design-system'`
  Props:
    - colorName: string (required) — The name of the color token to display
    - colorValue: string (required) — The hex value of the color token
    - size: 'small' | 'medium' | 'large' (optional) — The size of the swatch
- 'small': 40px
- 'medium': 60px
- 'large': 80px
    - className: string (optional) — Additional class names to apply to the swatch

### Input
  Input component
  Import: `import { Input } from 'test-client-design-system'`
  Props:
    - label: string (required) — The label for the input field
    - placeholder: string (optional) — The placeholder text for the input field
    - helperText: string (optional) — Helper text displayed below the input field
    - error: string (optional) — Error message displayed below the input field
    - disabled: boolean (optional) — Whether the input is disabled
    - size: 'small' | 'medium' | 'large' (optional) — The size of the input field
    - className: string (optional) — Additional class names to apply to the input field

### Card
  Card component
  Import: `import { Card } from 'test-client-design-system'`
  Props:
    - className: string (optional) — Optional class name for custom styling
    - padding: 'small' | 'medium' | 'large' (optional) — Padding size of the card
- 'small': 8px
- 'medium': 16px
- 'large': 24px
    - shadow: 'none' | 'small' | 'medium' | 'large' (optional) — Shadow variant of the card
- 'none': No shadow
- 'small': Small shadow
- 'medium': Medium shadow
- 'large': Large shadow
    - borderRadius: 'none' | 'small' | 'medium' | 'large' (optional) — Border radius of the card
- 'none': No border radius
- 'small': Small border radius
- 'medium': Medium border radius
- 'large': Large border radius
    - header: ReactNode (optional) — Optional header content
    - footer: ReactNode (optional) — Optional footer content
    - children: ReactNode (required) — Main content of the card

### Badge
  Badge component
  Import: `import { Badge } from 'test-client-design-system'`
  Props:
    - children: React.ReactNode (required) — The content to display inside the badge.
    - size: 'small' | 'medium' | 'large' (optional) — The size of the badge.
- 'small': Small size badge
- 'medium': Medium size badge
- 'large': Large size badge
    - variant: 'default' | 'primary' | 'success' | 'warning' | 'error' (optional) — The variant of the badge.
- 'default': Default styling
- 'primary': Primary styling
- 'success': Success styling
- 'warning': Warning styling
- 'error': Error styling
    - disabled: boolean (optional) — If true, the badge will appear disabled.
    - className: string (optional) — Additional class names to apply to the badge.

### Alert
  Alert component
  Import: `import { Alert } from 'test-client-design-system'`
  Props:
    - variant: 'info' | 'success' | 'warning' | 'error' (required) — The variant of the alert, determining its color scheme.
    - size: 'small' | 'medium' | 'large' (optional) — The size of the alert, determining its padding and font size.
    - className: string (optional) — Optional className for additional styling.

### Avatar
  Avatar component
  Import: `import { Avatar } from 'test-client-design-system'`
  Props:
    - src: string (optional) — The URL of the image to display as the avatar.
    - initials: string (optional) — The initials to display if the image is not available.
    - size: 'small' | 'medium' | 'large' (optional) — The size of the avatar.
    - shape: 'circle' | 'rounded' | 'square' (optional) — The shape of the avatar.
    - className: string (optional) — Additional class names to apply to the avatar.

## Token Summary
- Colors: 28 tokens
- Typography: 1383 tokens

## Component Conventions
- All components accept a `className` prop for overrides
- Use `forwardRef` for DOM element access
- Include JSDoc for all public props
